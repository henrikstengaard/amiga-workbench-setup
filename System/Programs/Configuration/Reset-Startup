; Reset Startup Script
; --------------------
; Author: Henrik Noerfjand Stengaard
; Date: 2016-09-06
;
; This script reset configuration of startup sequence Blizzard, ACA, Furia, CPU, FPU, Amiga Boot Selector and AGS2 for games and demos. 

.KEY noprompt/s
.BRA { 
.KET }  


; No prompt of reset configuration dialog
if "{noprompt}" not eq ""
	SKIP reset
EndIf


; Reset configuration
setenv reset `RequestChoice "Reset configuration" "Are you sure you want to reset startup configuration?" "Yes|No"`
If $reset eq "0" 
	SKIP end
EndIf


; Reset
LAB reset


; Reset Blizzard
IF EXISTS S:blizzard_run
	delete >NIL: S:blizzard_run
EndIF
IF EXISTS S:blizzard_a1200_ks31_maprom
	delete >NIL: S:blizzard_a1200_ks31_maprom
EndIF


; Reset ACA
IF EXISTS S:aca_run
	delete >NIL: S:aca_run
EndIF
IF EXISTS S:aca_a600_ks31_maprom
	delete >NIL: S:aca_a600_ks31_maprom
EndIF
IF EXISTS S:aca_a1200_ks31_maprom
	delete >NIL: S:aca_a1200_ks31_maprom
EndIF


; Reset Furia
IF EXISTS S:furia_run
	delete >NIL: S:furia_run
EndIF
IF EXISTS S:furia_a600_ks31_maprom
	delete >NIL: S:furia_a600_ks31_maprom
EndIF


; Reset CPU
Copy KingCON1.3/Handler/KingCON-handler L:KingCon-handler


; Reset FPU
Copy >NIL: guigfx_render_nofpu/ Libs:


; Reset Amiga Boot Selector
IF EXISTS S:abs_run
	delete >NIL: S:abs_run
EndIF


; Reset AGS2 Games
IF EXISTS S:ags2games_run
	delete >NIL: S:ags2games_run
EndIF


; Reset AGS2 Demos
IF EXISTS S:ags2demos_run
	delete >NIL: S:ags2demos_run
EndIF


; End
LAB end
